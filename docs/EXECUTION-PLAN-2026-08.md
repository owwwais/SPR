# خطة التنفيذ وتوزيع المهام — جولة أغسطس 2026

**التاريخ:** 28 أغسطس 2026 · **الفرع:** `claude/audit-august-2026-pajah5`
**موعد التنفيذ المجدول:** `2026-08-29T01:02Z` (بعد 4 ساعات) · المعرّف `trig_012ws8dP1PhatuEFxnmFDNYc`

**التفويض:** تنفيذ كل ما ورد في وثائق الجولة **عدا `AI-PROVIDER-2026-08.md`**، باتخاذ القرارات الهندسية ذاتياً ودون العودة للمالك. ما لا يمكن حسمه إلا بالمالك **يُؤجَّل صراحةً** ويُنتقل للبند التالي.

---

## النطاق

| المصدر | يُنفَّذ | لا يُنفَّذ |
|---|---|---|
| `AUDIT-2026-08.md` | كل بنود الأمن والسلامة والجودة القابلة للتنفيذ | `S1` موقع البيانات · `S10` مستوى المفتاح (كلاهما يحتاج حسابات المالك) |
| `PERF-2026-08.md` | الخيار (أ) عمود `fit_score` · الخيار (ب) تجميع الإحصائيات | (ج) رحلتا المصادقة — **متعمَّد**، حذفها يكسر تجديد الجلسة · (د) أوزان الخطوط (قرار تصميم) |
| `COMPETITIVE-2026-08.md` | التوصيات الثلاث كاملة | ما دونها من الاثنتي عشرة |
| `TALENT-PLATFORM-DESIGN.md` | الحد الأدنى القابل للإطلاق (§8) | المطابقة · الدعوات · البحث |
| `AI-PROVIDER-2026-08.md` | ❌ **لا شيء إطلاقاً** | تبديل النموذج · Bedrock · الدفعات · طبقة المحوّل |

---

## توزيع المهام — سبع دفعات مرتّبة بالقيمة

**الترتيب مقصود:** الأخطر والأوضح أولاً، فإن نفد الوقت يكون الأهم منجَزاً.

### 🔴 الدفعة 1 — تحصين أمني
| البند | المصدر | العمل |
|---|---|---|
| `S2` | تدقيق | `clientIp()` يأخذ **أيمن** وسيط موثوق لا أيسره — يغلق تجاوز حدّ المعدل |
| `S3` | تدقيق | Turnstile بلا مفتاح: يمرّ لكن **يسجّل خطأً صريحاً** بدل الصمت |
| `S4` | تدقيق | حذف رابط المشروع ومفتاح anon من هجرة `0004` → `current_setting()` |
| `S6` | تدقيق | حذف `image/svg+xml` من قائمة `org-assets` البيضاء |
| `S7` | تدقيق | فحص البصمة الثنائية (`%PDF`, `PK\x03\x04`) لا إقرار المتصفح |
| `R2` | تدقيق | إنهاء سباق التحليل المزدوج: `update … where analysis_status='pending' returning id` |
| `T1` | تدقيق | ترقية Next.js (ثغرة تجاوز الوسيط العالية) |
| `T2` | تدقيق | نقل `shadcn` إلى `devDependencies` — يحذف 4 حزم مصابة من الإنتاج |

**معيار القبول:** `npm audit` بلا ثغرات عالية في تبعيات الإنتاج · البناء و`tsc` و`eslint` نظيفة · `npm run test:db` يمرّ.

### 🔴 الدفعة 2 — صحة البيانات والأداء
| البند | العمل |
|---|---|
| `PERF` (ب) | دالة SQL تُجمّع الإحصائيات — **تُنهي البتر الصامت عند 5,000 وعرض أرقام خاطئة** |
| `PERF` (أ) | عمود `applications.fit_score` + trigger من `ai_evaluations` + فهرس مركّب + تعبئة رجعية |

**معيار القبول:** الإحصائيات صحيحة عند 8,000+ طلب · استعلام المرشحين لا ينمو خطياً — **قياس قبل/بعد بالأرقام على نفس قاعدة الـ13,000 طلب**.

### 🟠 الدفعة 3 — فرض الحصة (`R1` / `D16`)
حصة شهرية لكل شركة تُفحَص **داخل `analyze-application` قبل نداء النموذج**. صفحة الأسعار تبيع باقات لا يفرضها أحد اليوم.

### 🟢 الدفعة 4 — تصدير تقرير الشفافية (توصية المنافسين #1)
تصدير لكل قرار توظيف: الدرجة، التبرير، `prompt_version`، `model`، ومن غيّر الحالة ومتى من `status_history`. **`D24` يَعِد به ولم يُبنَ، والمكوّنات كلها موجودة — تجميع لا بناء.**

### 🟢 الدفعة 5 — الفرز المُعمّى + مؤشر السعودة (#2 و #3)
- **المُعمّى:** مرحلة تجهيل قبل التقييم، **اختيارية لكل شركة** (§القرار ه‍).
- **السعودة:** نطاق يُدخَل يدوياً، يظهر **عند مرحلة العرض فقط**، و**خارج درجة الملاءمة تماماً**.

### 🔵 الدفعة 6 — منصة المبدعين والعاملين (الحد الأدنى)
```
رفع + بريد → تحقق بريدي (يبوّب التحليل المدفوع) → تحليل → مراجعة وتصحيح
→ موافقتان منفصلتان → صفحة عامة برابط عشوائي
   ↕ إخفاء مؤقت · حذف نهائي فوري · منع فهرسة
```
+ التصنيف المعياري للمهارات · ضوابط إساءة الاستخدام كاملة (§7 من وثيقة التصميم).
**بلا مطابقة وبلا دعوات** — تحتاجان كثافة غير موجودة يوم الإطلاق.

### ⚪ الدفعة 7 — تلميع
`T4` اتجاه RTL في `components/ui/*` · `T6` صفحتا 404/500 بالعربية · `T7` حالات الخطأ بدل «لا نتائج» · تحديث `CLAUDE.md` (`T3`).

---

## القرارات الهندسية المُتّخَذة مسبقاً

اتُّخذت الآن حتى لا يتوقف التنفيذ المجدول ليسأل.

| # | القرار | السبب |
|---|---|---|
| **أ** | **البدء عند موعد الجدولة لا قبله** | صحّحه المالك صراحةً حين أوقف تعديلاً مبكراً. الجدولة نقطة بدء لا نقطة تحقّق |
| **ب** | فهرس `fit_score` بـ`CREATE INDEX` عادي لا `CONCURRENTLY` | الهجرات تعمل داخل معاملة، و`CONCURRENTLY` لا يعمل داخلها. البيانات اليوم صغيرة (قبل الإطلاق)؛ ستُوثَّق الطريقة اليدوية لقاعدة كبيرة |
| **ج** | `fit_score` عمود مشتق يملأه **trigger** لا كود التطبيق | مصدر واحد للحقيقة يمنع عدم التزامن؛ نفس نمط `applications_set_org` القائم |
| **د** | الإحصائيات عبر دالة `security definer` مقيَّدة بـ`current_org_ids()` | يحفظ عزل المستأجرين بينما ينقل التجميع للقاعدة |
| **ه‍** | **الفرز المُعمّى اختياري لكل شركة** (`blind_screening` افتراضياً `false`) | يضاعف كلفة النداء. جعله اختيارياً يحفظ الفاتورة **ويحوّله إلى ميزة تُباع** |
| **و** | **موجّه تجهيل جديد منفصل، والموجّه التقييمي يبقى حرفياً كما هو** | §10.8 يمنع تعديل الرُّبريك ذاتياً. إضافة موجّه جديد بإصداره الخاص تحترم القيد |
| **ز** | منصة المواهب: **مجموعة مسارات في نفس التطبيق + مخطط `talent` منفصل في القاعدة** | ⚠️ **انحراف عن وثيقة التصميم** التي رجّحت تطبيقاً منفصلاً. جوهر قلقها **عزل البيانات** — والمخطط المنفصل بسياساته يحقّقه. النشر المنفصل يضيف كلفة تشغيل بلا عزل إضافي، ويخالف «التبسيط هو القيمة المضافة». **قاعدة صارمة تبقى: لا `join` مباشر بين `public` و`talent`** |
| **ح** | تصنيف مهارات **مبدئي نبنيه** (~200 مهارة ثنائية اللغة) لا ESCO | ESCO يحتاج تحقّقاً من الرخصة والتغطية العربية — لا أبني على غير متحقَّق ولا أوقف العمل لأجله |
| **ط** | لا تُخزَّن «درجة مجال» ثابتة للموهوب إطلاقاً | §3 من وثيقة التصميم: الدرجة نسبية لوظيفة، والدرجة المجردة **خاطئة تقنياً** قبل أن تكون ضارّة |
| **ي** | رحلتا المصادقة تبقيان | حذف نداء الوسيط يكسر تجديد رمز الجلسة (`@supabase/ssr`) — الإصلاح إعادة هيكلة لا تنظيف |

## قرارات مؤجَّلة — للمالك وحده

| البند | لماذا لا أحسمه | البديل المتَّبع |
|---|---|---|
| `S1` **موقع البيانات (سيول)** | يحتاج حساب Supabase وهجرة بيانات | يبقى بنداً مفتوحاً في التقرير النهائي بأعلى أولوية |
| `S10` مستوى مفتاح Gemini | يحتاج لوحة Google Cloud | — |
| تسعير الحصص وأرقام الباقات | قرار تجاري | تُبنى الآلية بأرقام افتراضية قابلة للتعديل |
| العمود الأول لمنصة المواهب · مجانيتها | قرار سوق | تُبنى محايدة |
| أوزان الخطوط · الهوية البصرية | قرار تصميم | — |

---

## قواعد التنفيذ

1. **commit لكل دفعة**، برسالة تشرح ما تغيّر ولماذا.
2. **لا يُترك بند نصف منفَّذ.** إن تعذّر بند، يُسجَّل سبب التعذّر ويُنتقل للتالي.
3. **البناء و`tsc` و`eslint` و`test:db` تمرّ قبل كل دفع.**
4. **لا يُلمَس شيء من `AI-PROVIDER-2026-08.md`.**
5. **لا توقّف للسؤال.** كل قرار يخصّ العمل يُتَّخذ ويُسجَّل.
